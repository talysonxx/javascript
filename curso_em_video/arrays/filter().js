// filter, filtrar o seu array, retornar false ou true
/*var numbers = [1, 2, 4, 5, 6, 10]
var mult2= []

// verificando quais números no array numbers são multiplos de 2
for (posição in numbers) {
    if (numbers[posição] % 2 == 0) {
        mult2.push(numbers[posição])
    } else {
    }
}

console.log('Números mult de 2 do array: ', mult2)


// usando o filter pra fazer o mesmo
var filtrados = numbers.filter(function (numero) {
    return numero % 2 == 0
})

console.log(filtrados)

// multiplicando os números filtrados
numerosFiltradosMult2 = filtrados.map(function (numerosFiltrados) {
    return numerosFiltrados * 10
})

console.log(numerosFiltradosMult2)*/