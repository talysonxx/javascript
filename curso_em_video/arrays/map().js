/*var array = [1, 2, 3, 4, 5]
var novoArray = []

for (let numero in array) {
    novoArray.push(array[numero] * 2)
}

console.log('Array normal: ', array)
console.log('Array novo: ', novoArray)

// ultilizando map para fazer o mesmo
// o map espera uma função, o parâmetro numero vai receber cada valor do array
console.log(array.map(function (numero) { return numero * 10 }))

var arrayNomes = ['Talyson', 'André', 'Alves']

console.log(arrayNomes.map(function (nomes) { return nomes.toLowerCase()}))*/